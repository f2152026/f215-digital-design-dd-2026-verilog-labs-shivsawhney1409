// alu.v
// 1-bit-opcode ALU: op=0 -> add, op=1 -> sub. 4-bit operands.
// Subtraction is implemented the way real hardware does it: negate b (one's
// complement, then +1 for two's complement) and add.
//
// Switching only op originally left the previous result unchanged.
// Include op in the explicit sensitivity list so either operation updates.
// Non-blocking assignments also made subtraction use old intermediate values;
// blocking assignments make each dependent step use the value just computed.

module alu (
  input      [3:0] a,
  input      [3:0] b,
  input             op,      // 0 = add, 1 = sub
  output reg [3:0] result
);

  reg [3:0] b_inv;
  reg [3:0] b_twos;

  always @(a, b, op) begin
    case (op)
      1'b0: begin
        result = a + b;                 // add
      end
      1'b1: begin
        b_inv  = ~b;                    // sub, via two's complement
        b_twos = b_inv + 4'b0001;
        result = a + b_twos;
      end
    endcase
  end

endmodule

